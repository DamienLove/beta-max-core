## 2024-05-22 - Improved Interactive Element Accessibility
**Learning:** Converting `div onClick` to `Link` or `button` is a high-impact a11y win but requires careful styling (e.g., adding `block`) to maintain layout.
**Action:** Always check if a clickable element should be a link or button and refactor early. Using `Link` improves keyboard nav instantly.
